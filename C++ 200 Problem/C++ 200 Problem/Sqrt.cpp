#include "pch.h"
#include <iostream>

using namespace std;

int main()
{
	cout << "4의 제곱근 : " << sqrt(4.0) << endl;
	cout << "16의 제곱근 : " << sqrt(16) << endl;
	cout << "64의 제곱근 : " << sqrt(64) << endl;
	cout << "256의 제곱근 : " << sqrt(256.0) << endl;

	return 0;
}

// C++은 실수, 정수 모두 인자로 허용된다.